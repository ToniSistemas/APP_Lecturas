<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_water_mariadb_connection_list" model="ir.ui.view">
    <field name="name">water.mariadb.connection.list</field>
    <field name="model">water.mariadb.connection</field>
    <field name="arch" type="xml">
      <list string="Conexiones MariaDB">
        <field name="name"/>
        <field name="host"/>
        <field name="port"/>
        <field name="database"/>
        <field name="user"/>
        <field name="active"/>
        <field name="last_test_ok"/>
        <field name="last_test_date"/>
      </list>
    </field>
  </record>

  <record id="view_water_mariadb_connection_form" model="ir.ui.view">
    <field name="name">water.mariadb.connection.form</field>
    <field name="model">water.mariadb.connection</field>
    <field name="arch" type="xml">
      <form string="Conexión MariaDB">
        <header>
          <button name="action_test_connection" type="object"
                  string="Probar conexión" class="btn-primary"/>
          <field name="active" widget="boolean_toggle"/>
        </header>
        <sheet>
          <group string="Conexión">
            <group>
              <field name="name"/>
              <field name="host"/>
              <field name="port"/>
              <field name="database"/>
            </group>
            <group>
              <field name="user"/>
              <field name="password" password="True"/>
            </group>
          </group>
          <group string="Última prueba">
            <field name="last_test_ok" readonly="1"/>
            <field name="last_test_date" readonly="1"/>
            <field name="last_test_message" readonly="1"/>
          </group>
        </sheet>
      </form>
    </field>
  </record>

  <record id="action_water_mariadb_connections" model="ir.actions.act_window">
    <field name="name">Conexiones MariaDB</field>
    <field name="res_model">water.mariadb.connection</field>
    <field name="view_mode">list,form</field>
  </record>

  <menuitem id="menu_water_mariadb_connections"
            name="Conexiones MariaDB"
            parent="menu_water_root"
            action="action_water_mariadb_connections"
            groups="base.group_system"
            sequence="9"/>
</odoo>